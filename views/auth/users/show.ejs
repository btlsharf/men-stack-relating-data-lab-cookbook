<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= user?.username || 'User' %>'s Pantry</title>
</head>
<body>
 
<h1><%= user?.username || 'User' %>'s Pantry</h1>

<ul>
  <% user?.pantry.forEach(item => { %>
    <li> <%= food.name %></li>
  <% }) %>
</ul>

<a href="/users">Back to Community</a>
</body>
</html>